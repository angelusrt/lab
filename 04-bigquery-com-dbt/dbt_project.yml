name: 'project'
version: '1.0.0'
profile: 'project'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  project:
    +schema: flows

    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table

      base:
        +materialized: table
        +schema: base
